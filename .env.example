# Base URL of the backend API (include the /api suffix if your backend uses it).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8080/api

# Backend origin, used to resolve uploaded media URLs and allow-list image hosts.
NEXT_PUBLIC_BACKEND_URL=http://localhost:8080
